name: Automated-CI-Linux

on:
  workflow_dispatch:
    inputs:
      tag_name:
        description: "Tag name for release. If set to nightly, the release will be a pre-release."
        required: false
        default: nightly

jobs:
  Automated-CI-Linux:
    runs-on: ubuntu-latest
    container:
      image: archlinux:base-devel
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Install Flutter Action Dependencies
        run: |
          pacman -Syu
          pacman -S jq
      - name: Install Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      - name: Install Flutter Linux Dependencies
        run: |
          pacman -S gtk3 libsecret gnome-keyring wpewebkit git clang cmake ninja
      - name: Prepare Flutter Dependencies
        run: |
          flutter pub get
          flutter pub global activate intl_utils
          flutter pub global run intl_utils:generate

      - name: Build Executable
        run: |
          dart build_release.dart --target linux --versionCode dummy

      - if: github.event_name == 'workflow_dispatch' && github.event.inputs.tag_name != 'nightly'
        name: Release
        uses: softprops/action-gh-release@v1
        with:
          files: build/app/DanXi-dummy-release.linux-x64.zip
          prerelease: false
          tag_name: ${{ github.event.inputs.tag_name }}
          name: Release ${{ github.ref }}
          generate_release_notes: true
          fail_on_unmatched_files: true

      - if: github.event_name == 'workflow_dispatch' && github.event.inputs.tag_name == 'nightly'
        name: Release Nightly
        uses: softprops/action-gh-release@v1
        with:
          files: build/app/DanXi-dummy-release.linux-x64.zip
          prerelease: true
          tag_name: nightly
          name: Nightly build
          generate_release_notes: true
          fail_on_unmatched_files: true
